var frontend    = require('./frontend'),
    feeds       = require('./feeds'),
    redirects   = require('./redirects'),
    admin       = require('./admin')
    api         = require('./api');

module.exports = {
    frontend:   frontend,
    feeds:      feeds,
    redirects:  redirects,
    admin:      admin,
    api:        api
};

// feeds/posts/default
/////
